on:
  issues:
    types: [opened]
name: Issues
jobs:
  assignAuthor:
    name: Assign author to issue
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
      - name: Assign author to issue
        uses: technote-space/assign-author@v1
